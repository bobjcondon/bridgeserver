# CRUD operations

Add CRUD operations and routes for the locations and tournamets tables (in src/lib/server/db/schema.ts).   Use the same conventions you used for the players table.
Tell me which skill(s) you used.