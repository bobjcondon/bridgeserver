# Signin Button

Make a signin/signout button and add it to left of the Github button in Header.svelte

If a user is currently logged in  button shows "SignOut" and, when pressed, logs out current user.
If no user is currently logged in button shows "SignIn" and, when pressed pressed, goes to the src/routes/better-auth/login route.

