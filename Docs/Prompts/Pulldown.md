# Pulldown Menus

For each Table T in the CRUD tables ( plarers, locations, tournaments} make a CRUD-route button which links to the routes associated with T.
Add these buttons to the Hedaer.svelte component.  They should come after the "about" link.


