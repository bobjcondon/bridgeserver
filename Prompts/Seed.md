# Seed the Database

You are a expert drizzle user.   
Examine the schema in src/lib/server/schema.ts and write a script which will 
initialize the database with 5 users, 10 players, 2 locations and 2 tournaments.   
The 5 users should have a corresponding player (user.email == player.email).   
Update the package.json file to allow running the script.  